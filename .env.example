# Environment variables for Vercel deployment

# Database connection (Supabase Postgres)
# Get this from Supabase Project -> Settings -> Database -> Connection string (Direct connection)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_ID.supabase.co:5432/postgres

# Optional: Flask secret key (will use default if not provided)
SECRET_KEY=your-secret-key-here

# OpenAI API for translation and completion features
OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_API_BASE=https://api.openai.com/v1  # Optional: use different API endpoint

# Optional: Supabase settings (for future direct API usage)
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_KEY=your-anon-key
# OPENAI_MODEL=gpt-3.5-turbo
